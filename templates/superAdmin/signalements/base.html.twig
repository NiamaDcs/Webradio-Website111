<!DOCTYPE html>
<html lang="en">
    {% include 'superAdmin/head.html.twig' %}
    {% include 'superAdmin/Signalements/script.html.twig' %}
<body id="page-top">

<!-- Page Wrapper -->
<div id="wrapper">
    {% include 'superAdmin/sidebar.html.twig' %}
        <!-- Main Content -->
        <div id="content">
            {% include 'superAdmin/navigation.html.twig' %}
            <!-- Begin Page Content -->
            <div class="container-fluid">

                <div class="row mt-4 mb-2">
                    <div class="col-xl-2">
                        <h4 class="h4 text-gray-800">Signalements</h4>
                    </div>
                    <div class="col-xl-10 d-flex"> 

                     {#{ form_start(form) }}
                        <!-- Search form -->
                        <div class="form-group has-search user">
                            <button class="fa fa-search form-control-feedback"></button>
                             {{ form_row(form.channels) }}
                        </div>
                      {{ form_end(form) }#}
                    </div>
                </div>
                <div class="row">
                    <!-- table -->
                    <div class="col-xl-12">
                        <div class="card shadow mb-4">
                            <!-- Card Body -->
                            <div class="card-body">
                                <div class="row col-sm-12">
                                    <div class="table-responsive">
                                
                                    <table id="table" class"table table-bordered w-100" role="grid">
                                    <thead>
                                        <tr id='entete' role="row">
                                            <th scope='col'>Avatar</th>
                                            <th scope='col'>Nom de la chaîne</th>
                                            <th scope='col'>Nombre de signalements</th>
                                            <th scope='col'>Liste des signalements</th>
                                            <th scope='col'>Management</th>
                                        </tr>
                                    </thead>
                                    <tbody id='corps-table'>
                                         {% for Signalement in Signalements %}
                                            <tr>
                                                <td><img class="rounded-circle" style="font-size:2em; width:31px; height:32px;" src="{{ Signalement.avatar }}" alt="" id="profil"></td>
                                                <td>{{ Signalement.nomChaine }}</td>
                                                <td>{{ Signalement.nombreSignal }}</td>
                                                <td><a href='{{ path('superadmin.Signalements.show', {slug: Signalement.slug, id: Signalement.id}) }}' >{{ Signalement.listSignal }}</a></td>
                                                <td>
                                                   <a href='#bannirModal' data-toggle='modal' data-dismiss='modal' class='btn text-danger'>{{ Signalement.management }}</a>
                                                </td>
                                            </tr>
                                            {% include 'superAdmin/signalements/bannir.html.twig' %}
                                         {% endfor %}
                                    </tbody>
                                    </table>
                                    <div class="pagination mt-2 d-flex justify-content-center" id="pagination_data">
                                        {{ knp_pagination_render(Signalements) }}
                                    </div>      
                                  </div>
                                </div>                                
                            </div>
                        </div>
                    </div>
                </div>
            </div>
            <!-- /.container-fluid -->

       

            <!-- Footer -->
            <footer class="sticky-footer bg-white">
                <div class="container my-auto">
                    <div class="copyright text-center my-auto">
                        <span class="text-black">Copyright &copy; Webradio - Stream - 2020</span>
                    </div>
                </div>
            </footer>
            <!-- End of Footer -->
        </div>
        <!-- End of Content Wrapper -->

    </div>
<!-- End of Page Wrapper -->

<!-- Scroll to Top Button-->
<a class="scroll-to-top rounded" href="#page-top">
    <i class="fas fa-angle-up"></i>
</a>


</body>

</html>